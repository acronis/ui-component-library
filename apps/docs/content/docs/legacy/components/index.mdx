---
title: Components
description: Browse the legacy shadcn-uikit components. Deprecated — prefer @spec-lab/ui-react.
---

import { Callout } from 'fumadocs-ui/components/callout';

<Callout type="warn" title="Legacy components">
  These pages document `@spec-lab/shadcn-uikit`, which is in
  maintenance/freeze. For new work, use the
  [`@spec-lab/ui-react` components](/components).
</Callout>

The legacy shadcn-uikit provides 40+ production-ready React components. Each component page includes live demos, source code, and API reference.

## Installation

```bash
npm install @spec-lab/shadcn-uikit tw-animate-css
```

```tsx
import '@spec-lab/shadcn-uikit/styles';
import { Button } from '@spec-lab/shadcn-uikit';
```

Browse the components using the sidebar.
