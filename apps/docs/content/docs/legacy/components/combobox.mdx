---
title: Combobox
description: A searchable dropdown for selecting from a list of options.
---

import {
  ComboboxBasic,
  ComboboxWidths,
  ComboboxSmall,
  ComboboxForm,
  ComboboxDisabled,
} from '@/components/demos/combobox';
import { DemoPreview } from '@/components/DemoPreview';

## Usage

```tsx
import { Combobox } from '@spec-lab/shadcn-uikit';
```

## Basic

A basic searchable select dropdown.

<DemoPreview sourcePath="apps/demos/src/combobox/ComboboxBasic.tsx">
  <ComboboxBasic />
</DemoPreview>

## Widths

Combobox with different width configurations.

<DemoPreview sourcePath="apps/demos/src/combobox/ComboboxWidths.tsx">
  <ComboboxWidths />
</DemoPreview>

## Small

A compact combobox variant for tighter layouts.

<DemoPreview sourcePath="apps/demos/src/combobox/ComboboxSmall.tsx">
  <ComboboxSmall />
</DemoPreview>

## In a form

Combobox integrated with form validation.

<DemoPreview sourcePath="apps/demos/src/combobox/ComboboxForm.tsx">
  <ComboboxForm />
</DemoPreview>

## Disabled state

A disabled combobox that cannot be interacted with.

<DemoPreview sourcePath="apps/demos/src/combobox/ComboboxDisabled.tsx">
  <ComboboxDisabled />
</DemoPreview>

## API Reference

{/* No exported Props interface — Combobox is a composition pattern, not a reusable component with props */}

The Combobox is a composition of `Command` and `Popover` primitives. It does not export a standalone component with its own Props interface. See the source code for implementation patterns using `Command`, `CommandInput`, `CommandList`, `CommandItem`, `Popover`, `PopoverTrigger`, and `PopoverContent`.
