---
title: Form
description: Handles data entry with validation and submission using react-hook-form.
---

import {
  FormBasic,
  FormLogin,
  FormProfile,
  FormRegistration,
  FormContact,
  FormSettings,
  FormTanstackBasic,
  FormTanstackProfile,
  FormLayoutTwoColumn,
  FormLayoutSections,
} from '@/components/demos/form';
import { DemoPreview } from '@/components/DemoPreview';

## Usage

```tsx
import {
  Form,
  FormField,
  FormItem,
  FormLabel,
  FormControl,
  FormMessage,
} from '@spec-lab/shadcn-uikit';
```

## Basic

A simple form with a single field and validation.

<DemoPreview sourcePath="apps/demos/src/form/FormBasic.tsx">
  <FormBasic />
</DemoPreview>

## Login

A login form with email and password fields.

<DemoPreview sourcePath="apps/demos/src/form/FormLogin.tsx">
  <FormLogin />
</DemoPreview>

## Profile

A profile editing form with multiple field types.

<DemoPreview sourcePath="apps/demos/src/form/FormProfile.tsx">
  <FormProfile />
</DemoPreview>

## Registration

A user registration form with validation rules.

<DemoPreview sourcePath="apps/demos/src/form/FormRegistration.tsx">
  <FormRegistration />
</DemoPreview>

## Contact

A contact form with message textarea.

<DemoPreview sourcePath="apps/demos/src/form/FormContact.tsx">
  <FormContact />
</DemoPreview>

## Settings

A settings form with switches and toggles.

<DemoPreview sourcePath="apps/demos/src/form/FormSettings.tsx">
  <FormSettings />
</DemoPreview>

## Layout: Two-column grid

Side-by-side pairs for related fields.

<DemoPreview sourcePath="apps/demos/src/form/FormLayoutTwoColumn.tsx">
  <FormLayoutTwoColumn />
</DemoPreview>

## Layout: Sectioned form

Long forms split into labelled sections with dividers.

<DemoPreview sourcePath="apps/demos/src/form/FormLayoutSections.tsx">
  <FormLayoutSections />
</DemoPreview>

## TanStack Form — Basic

The lower-level `FormItem` primitives work with any form library. This example uses [`@tanstack/react-form`](https://tanstack.com/form) with Zod validation.

<DemoPreview sourcePath="apps/demos/src/form/FormTanstackBasic.tsx">
  <FormTanstackBasic />
</DemoPreview>

## TanStack Form — Profile

Multiple field types (input, select, textarea) wired with TanStack Form.

<DemoPreview sourcePath="apps/demos/src/form/FormTanstackProfile.tsx">
  <FormTanstackProfile />
</DemoPreview>

For a full guide covering layout patterns, component reference, validation strategies, and accessibility, see the [Forms Guide](/guides/forms).

## API Reference

{/* No exported Props interface — Form sub-components use react-hook-form and native HTML element props */}

The `Form` component is a re-export of `react-hook-form`'s `FormProvider`. `FormField` accepts `ControllerProps` from `react-hook-form`. Sub-components `FormItem`, `FormLabel`, `FormControl`, `FormDescription`, and `FormMessage` extend their native HTML element props and connect to form context via the `useFormField` hook.
