---
title: Command
description: A command palette — a searchable, grouped list of commands.
---

import { DemoReact } from '@/components/DemoReact';
import {
  CommandDemo,
  CommandDialogDemo,
} from '@/components/demos-react/command';

## Usage

```tsx
import { Command, CommandDialog } from '@spec-lab/ui-react';
```

`Command` is a command palette — a searchable, filtered list of grouped commands
with keyboard navigation and per-command shortcuts. It's **data-driven**: pass
grouped `commands` and an `onSelect`. Built on the Base UI `Combobox` primitive
(filtering, keyboard nav, and combobox/listbox ARIA come from Base UI) — a
**design-pending v1** ported from the legacy kit, with no `cmdk` dependency.

Use it inline, or as a ⌘K overlay via `CommandDialog`.

## Examples

Inline palette:

<DemoReact>
  <CommandDemo />
</DemoReact>

```tsx
const commands = [
  {
    heading: 'Suggestions',
    items: [
      { value: 'calendar', label: 'Calendar', icon: <CalendarIcon /> },
      { value: 'search', label: 'Search', shortcut: '⌘F' },
    ],
  },
  {
    heading: 'Settings',
    items: [
      { value: 'profile', label: 'Profile', shortcut: '⌘P' },
      { value: 'billing', label: 'Billing', disabled: true },
    ],
  },
];

<Command commands={commands} onSelect={(value) => run(value)} />;
```

### ⌘K overlay

Wrap it in `CommandDialog` for the classic command-menu overlay. Run the command
from `onSelect` and close the dialog:

<DemoReact>
  <CommandDialogDemo />
</DemoReact>

```tsx
const [open, setOpen] = React.useState(false);

<CommandDialog
  open={open}
  onOpenChange={setOpen}
  commands={commands}
  onSelect={(value) => {
    run(value);
    setOpen(false);
  }}
/>;
```

## API Reference

<AutoTypeTable
  path="../../packages/ui-react/src/components/ui/command/command.docs.ts"
  name="CommandProps"
/>

`CommandDialog` adds the dialog controls:

<AutoTypeTable
  path="../../packages/ui-react/src/components/ui/command/command.docs.ts"
  name="CommandDialogProps"
/>

Each group and command:

<AutoTypeTable
  path="../../packages/ui-react/src/components/ui/command/command.docs.ts"
  name="CommandGroup"
/>

<AutoTypeTable
  path="../../packages/ui-react/src/components/ui/command/command.docs.ts"
  name="CommandOption"
/>
