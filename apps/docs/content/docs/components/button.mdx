---
title: Button
description: Triggers an action or event.
---

import { DemoReact } from '@/components/DemoReact';
import { ButtonDemo } from '@/components/demos-react/button';

## Usage

```tsx
import { Button } from '@constructor-lab/ui-react';
```

`Button` renders a native `<button>` styled by the `--ui-button-*` design
tokens. It is polymorphic via Base UI's `useRender` — pass the `render` prop to
render it as another element (e.g. a router link) while keeping the button
styling and merged props.

## Examples

Variants map to the Figma "Button" set. The default is `default` (primary):

<DemoReact>
  <ButtonDemo />
</DemoReact>

```tsx
<Button variant="default">Save</Button>
<Button variant="secondary">Cancel</Button>
<Button variant="ghost">Learn more</Button>
<Button variant="destructive">Delete</Button>
<Button variant="ai">Ask AI</Button>
<Button variant="inverted">On a dark surface</Button>
```

The `ai` variant automatically leads with a Sparkles icon. Other variants accept
an icon as part of their children:

```tsx
import { PlusIcon } from '@constructor-lab/icons-react/stroke-mono';

<Button variant="default">
  <PlusIcon />
  Add item
</Button>;
```

Disabled state uses dedicated disabled tokens (not opacity):

```tsx
<Button variant="default" disabled>
  Save
</Button>
```

Render as another element with `render` (Base UI composition):

```tsx
<Button render={<a href="/dashboard" />}>Go to dashboard</Button>
```

## API Reference

<AutoTypeTable
  path="../../packages/ui-react/src/components/ui/button/button.tsx"
  name="ButtonProps"
/>
