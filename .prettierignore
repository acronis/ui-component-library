dist
node_modules
*.min.js
pnpm-lock.yaml

# Next.js auto-generates this on every build; it owns its style.
apps/docs/next-env.d.ts

# Generated, committed token artifacts (built by tools/style-dictionary).
packages/tokens/css
packages/tokens/scss
packages/tokens/js
packages/tokens/dtcg

# Design-token source tiers are formatted by the figma-to-design-tokens skill
# emitters (DtcgFormatter), which own their house style. Keep Prettier out of them.
packages/tokens/tiers

# Emit golden fixtures are serialized DtcgFormatter output, byte-compared in the
# token-emit tests. Prettier rewraps them into a form the formatter never emits
# (breaking the tests), so keep Prettier out of them.
tools/token-emit/__tests__/fixtures/*.golden.json

# Generated (and gitignored) React icon packs, emitted by icons-react's
# `generate` script from @constructor-lab/icons-svg. Regenerated on every build/typecheck,
# so Prettier can never keep them formatted — leave them to the generator.
packages/icons-react/src/packs
