export type CheckboxOption = any;

// CheckboxModel is boolean when modelValue is Boolean
// CheckboxModel is Array<CheckboxOption>/Set<CheckboxOption> when modelValue is Array/Set
export type CheckboxModel = any;
