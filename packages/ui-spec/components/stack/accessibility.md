# Stack — accessibility

Stack is a presentational layout `<div>` with no semantics of its own. It does not
change reading order — children are laid out in source order. Choose a semantic
wrapper (e.g. `Section`, `<ul>`) where the content calls for one.

## Contrast

No color; nothing to evaluate.
