spec_version: '1.0'
component: Dialog
name: dialog
status: draft
category: overlays
layer: primitive
description: >-
  A modal overlay that interrupts the user to show focused content or request a
  decision. A set of composable parts (trigger, overlay, content, header, title,
  close, body, description, footer) built on the Base UI Dialog primitive. Initial
  version ported from the legacy shadcn-uikit `Dialog`; reconciled against Figma
  and themed from the dedicated `--ui-dialog-*` token tier.
since: 0.9.0
figma:
  node: '6343-58898'
  codeConnect: packages/ui-react/src/components/ui/dialog/dialog.figma.tsx
dependencies:
  components:
    - TimesIcon
