spec_version: '1.0'
component: Separator

# References only. Values live in @constructor-lab/tokens. No `--ui-separator-*`
# tier yet; the rule uses the shared divider token (bridged as bg-border).
source: '@constructor-lab/tokens'

tokens:
  - {
      name: --ui-border-on-surface-border,
      affects: root,
      description: Divider line color (bridged as bg-border),
    }
