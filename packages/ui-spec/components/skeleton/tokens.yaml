spec_version: '1.0'
component: Skeleton

# References only. Values live in @spec-lab/tokens.
source: '@spec-lab/tokens'

tokens:
  - name: --ui-background-surface-active
    affects: skeleton
    description: >-
      The placeholder fill — a visible gray (the muted surface is near-white and
      disappears on a white background).
