spec_version: '1.0'
component: Toast
name: toast
status: ready
category: feedback
layer: primitive
description: >-
  A transient notification shown in a corner stack — title, optional description,
  status icon, optional action, and a close button. Triggered imperatively via
  the `toast(...)` API and rendered by a single `<Toaster>` region. Ported from
  the legacy shadcn-uikit `sonner` wrapper; rebuilt on the Base UI toast manager.
  Bound to its own `--ui-toast-*` tier. A white surface with a
  strong status border, a 6px left accent bar, a full-color status icon, and a
  floating shadow. Aliased as `Notification` / `notification`.
since: 0.41.0
figma:
  node: '7421:126262'
  codeConnect: packages/ui-react/src/components/ui/toast/toast.figma.tsx
