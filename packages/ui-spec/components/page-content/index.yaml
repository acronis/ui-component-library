spec_version: '1.0'
component: PageContent
name: page-content
status: draft
category: layout
description: >-
  The page content region — the padded gutter for a page's body. A <div> meant to
  nest inside AppShellMain (so it doesn't duplicate the main landmark). Ported from
  the legacy shadcn-uikit `page-content`.
since: 0.52.0
