spec_version: '1.0'
component: DataTable

# DataTable is an engine/render-context layer and owns no visual tokens. Its view
# projects through Table, which owns the table-shaped presentation tokens.
# UIKit-composed controls and their tokens belong to DataGrid.
source: '@constructor-lab/tokens'

tokens: []
