spec_version: '1.0'
component: Collapsible

# Collapsible has no tokens of its own — it's a behavioral disclosure primitive
# with no color. Content styling comes from the caller / the component built on it
# (e.g. Accordion). Values live in @spec-lab/tokens-pd.
source: '@spec-lab/tokens-pd'

tokens: []
