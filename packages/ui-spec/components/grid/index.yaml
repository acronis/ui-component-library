spec_version: '1.0'
component: Grid
name: grid
status: draft
category: layout
description: >-
  A responsive CSS-grid layout primitive — lay children into a column count that
  steps down at smaller breakpoints, with a configurable gap. Ported 1:1 from the
  legacy shadcn-uikit `grid`.
since: 0.51.0
