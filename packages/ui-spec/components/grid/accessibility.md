# Grid — accessibility

Grid is a presentational layout `<div>` with no semantics of its own; children
follow source order. It uses CSS grid only for visual layout — don't rely on it to
convey relationships that need real table/list semantics.

## Contrast

No color; nothing to evaluate.
