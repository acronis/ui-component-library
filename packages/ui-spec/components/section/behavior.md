# Section — behavior

```gherkin
Scenario: Titled block
  Given a Section with a header (title + description) and content
  Then the title, muted description, and body render as a vertically spaced block
```
