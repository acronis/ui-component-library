# Local Figma token snapshots — reproducible pull inputs written by the
# sync flow (tools/figma-token-exporter → its receiver). Never committed; a
# contributor re-fetches them from Figma. The tier emitters now live in the
# /figma-to-design-tokens skill, so nothing else under .tmp/ is tracked.
.tmp/figma-tokens/
