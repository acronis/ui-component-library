{
  "name": "@constructor-lab/icons-svg",
  "version": "0.2.0",
  "private": true,
  "description": "Next-gen raw SVG icon sources for UI Components library — redesigned icons fetched from the shadcn-uikit Figma file, plus per-category JSON manifests.",
  "type": "module",
  "exports": {
    "./package.json": "./package.json",
    "./svg/*": "./src/svg/*.svg",
    "./figma/*": "./src/figma/*.json"
  },
  "scripts": {
    "build": "echo \"no build (raw SVG source package)\" && exit 0",
    "dev": "echo \"no dev server (raw SVG source package)\" && exit 0",
    "clean": "echo \"nothing to clean (raw SVG source package)\" && exit 0",
    "pull-icons": "tsx ../../tools/figma-icons-fetcher/src/index.ts",
    "lint": "echo \"no lint (raw SVG source package)\" && exit 0",
    "lint:fix": "echo \"no lint (raw SVG source package)\" && exit 0",
    "test": "echo \"no tests (raw SVG source package)\" && exit 0",
    "test:watch": "echo \"no watch mode (raw SVG source package)\" && exit 0",
    "typecheck": "echo \"no typecheck (raw SVG source package)\" && exit 0"
  },
  "keywords": [
    "acronis",
    "icons",
    "svg",
    "design-system"
  ],
  "devDependencies": {
    "@constructor-lab/figma-icons-fetcher": "workspace:*",
    "tsx": "^4.23.12"
  }
}
