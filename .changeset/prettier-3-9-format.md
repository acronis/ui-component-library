---
---

Repo-wide Prettier reformat (Prettier 3.8.3 → 3.9.5) and a new CI `format:check`
gate. Whitespace/formatting only — no source behavior, public API, or shipped
output changes — so this is an intentionally empty changeset (no release).
