---
---

No release. The published packages only changed their `repository.url`
metadata when the canonical remote moved to `acronis/ui-component-library`;
no source, types, or runtime behaviour changed. The corrected URL ships with
the next real release.
