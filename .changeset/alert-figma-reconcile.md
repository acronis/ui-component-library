---
'@spec-lab/ui-react': minor
---

Reconcile `Alert` with the Figma design and add a dismiss button. Retheme the variant border and icon to the correct status tokens (`--ui-border-on-status-*` subtle border + `--ui-glyph-on-status-*` icon) instead of the saturated `--ui-background-status-strong-*` fill, and add an optional `AlertClose` (×) part tinted by the variant. Completes the Figma Code Connect.
