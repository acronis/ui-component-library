name: Release Figma Fetcher

on:
  push:
    branches:
      - main
    paths:
      - 'packages/figma-fetcher/**'

permissions:
  contents: write

jobs:
  changelog:
    if: github.repository == 'acronis/ui-component-library'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Conventional Changelog
        id: changelog
        uses: TriPSs/conventional-changelog-action@v5.3.0
        with:
          preset: "conventionalcommits"
          github-token: ${{ secrets.ACV_TOKEN }}
          version-file: './packages/figma-fetcher/package.json'
          output-file: './packages/figma-fetcher/CHANGELOG.md'
          tag-prefix: 'figma-fetcher/'
